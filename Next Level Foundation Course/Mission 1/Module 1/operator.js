// Arithmetic operators  + - * / %
// Assignment operators = += -= *= /=
// comparison operators (===,==,<,>,<=,=>)
// Logical operators && || !

const a=10;
const b =3;

const sum = a+b;
//console.log(sum);

const divition = a/b;

//console.log(divition)

//console.log(divition.toFixed(2));

const rmndr = a%b;
//console.log (rmndr);

// a+b - (c+f) * d/e
// bracket at first, then mul or div going from left to right, then add or sub

//sum = sum +10 OR sum+=10;

//const c = a>b && 10;
//console.log(c);

const c = !(a<b) && 10;
console.log(c);